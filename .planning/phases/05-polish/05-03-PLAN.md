---
phase: 05-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified: []
autonomous: false
requirements:
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "User can export all 4 entity types as CSV and open in Excel correctly"
    - "Audit log has entries for note deletion, formula deletion, appointment cancellation, and no-show"
    - "iOS Safari PWA does not trap scroll behind keyboard in bottom sheets"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for complete Phase 5 — confirm CSV exports work in a real spreadsheet, audit log entries are created for all sensitive actions, and iOS Safari PWA keyboard behavior is correct.

Purpose: Phase 5 success criteria require real-world verification that cannot be fully automated: opening CSVs in Excel/Numbers to check formatting, querying the audit log after performing sensitive actions, and testing keyboard interaction on a real iOS device.

Output: Confirmed Phase 5 complete or issues list for gap closure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-01-SUMMARY.md
@.planning/phases/05-polish/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 5 — CSV exports, audit log, iOS PWA</name>
  <files></files>
  <action>
Human verification of all Phase 5 deliverables. No code changes — this is a verification-only checkpoint.
  </action>
  <what-built>
Complete Phase 5: CSV data export (4 entities), audit log completeness (deletions + cancellations), and iOS PWA keyboard fix.
  </what-built>
  <how-to-verify>
**CSV Export (DATA-02):**
1. Open the app and navigate to Settings
2. Scroll down to the "Export Data" section
3. Tap "Export Clients (CSV)" — verify a file downloads
4. Open the downloaded CSV in Excel or Numbers
5. Verify: column headers are present, data is correct, no garbled characters (especially any pound signs)
6. Repeat for Appointments — verify the "services" column shows pipe-separated service names
7. Repeat for Payments — verify "amount_gbp" column shows decimal GBP values (e.g., "25.00" not "2500")
8. Repeat for Notes — verify colour formulas appear with note_type "colour_formula"

**Audit Log (DATA-03):**
9. Delete a client note in the app
10. In Supabase dashboard, run: `select action, entity_type, entity_id, details from audit_log order by created_at desc limit 5`
11. Verify a row with action='note_deleted' and entity_type='client_note' exists
12. Cancel an appointment in the diary
13. Re-run the audit log query — verify a row with action='appointment_cancelled' exists
14. (Optional) Delete a colour formula and verify 'colour_formula_deleted' row appears

**iOS PWA (if iOS device available):**
15. Open the app in iOS Safari, add to home screen
16. Open the PWA from home screen
17. Open any bottom sheet with a text input (e.g., booking a new appointment with notes)
18. Tap into the text field — verify the keyboard opens without trapping scroll (you can still see and interact with the input field)
19. Verify the bottom nav clears the home indicator bar (already implemented, just confirm)

If no iOS device available, note which steps were skipped.
  </how-to-verify>
  <verify>Human confirms all verification steps pass or reports issues</verify>
  <done>Phase 5 approved — CSV exports, audit log, and iOS PWA verified by human</done>
  <resume-signal>Type "approved" to complete Phase 5, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All Phase 5 success criteria from ROADMAP verified:
1. User can export all their data (clients, appointments, payments, notes) as CSV files that open correctly in Excel
2. Every sensitive action (invoice adjustment, refund, deletion) appears in the audit log with user ID and timestamp
3. The app works correctly on iOS Safari as a home-screen PWA
</verification>

<success_criteria>
- Human confirms all 4 CSV exports download and open correctly
- Human confirms audit log entries exist for deletions and cancellations
- Human confirms iOS PWA keyboard behavior (or notes device unavailable)
- Phase 5 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-03-SUMMARY.md`
</output>
