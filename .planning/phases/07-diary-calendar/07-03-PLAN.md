---
phase: 07-diary-calendar
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - native/app/(tabs)/index.tsx
  - native/src/components/diary/SegmentedControl.tsx
  - native/src/components/diary/MonthView.tsx
  - native/src/components/diary/WeekView.tsx
  - native/src/components/diary/DayView.tsx
  - native/src/components/diary/TimeGrid.tsx
  - native/src/components/diary/CurrentTimeLine.tsx
  - native/src/components/diary/DateHeader.tsx
autonomous: true
requirements:
  - DIARY-01
  - DIARY-02
  - DIARY-03
  - DIARY-04
  - DIARY-05
  - DIARY-14
  - DIARY-15

must_haves:
  truths:
    - "User can see a month calendar with dots on days that have bookings"
    - "User can tap a day in month view to jump to that day's day view"
    - "User can see a 7-day week overview with appointment blocks"
    - "User can see a day view with a time slot grid and positioned appointment blocks"
    - "User can switch between Month, Week, Day views using a segmented control"
    - "User can swipe left/right to navigate between days (day view) and weeks (week view)"
    - "User can tap navigation arrows to go forward/backward"
    - "User can tap the date header to return to today"
    - "Current time is shown as a red horizontal line on today's day view"
    - "Day view auto-scrolls to current time (today) or working hours start (other days)"
  artifacts:
    - path: "native/app/(tabs)/index.tsx"
      provides: "DiaryScreen — main diary tab with view state, date state, data loading"
      min_lines: 100
    - path: "native/src/components/diary/SegmentedControl.tsx"
      provides: "Month/Week/Day tab switcher"
      exports: ["SegmentedControl"]
    - path: "native/src/components/diary/MonthView.tsx"
      provides: "react-native-calendars Calendar with dot indicators"
      exports: ["MonthView"]
    - path: "native/src/components/diary/WeekView.tsx"
      provides: "7-column week layout with appointment blocks"
      exports: ["WeekView"]
    - path: "native/src/components/diary/DayView.tsx"
      provides: "ScrollView time grid with positioned appointment blocks"
      exports: ["DayView"]
    - path: "native/src/components/diary/TimeGrid.tsx"
      provides: "Shared time grid rendering (slot rows + appointment blocks)"
      exports: ["TimeGrid"]
    - path: "native/src/components/diary/CurrentTimeLine.tsx"
      provides: "Red current-time indicator line"
      exports: ["CurrentTimeLine"]
    - path: "native/src/components/diary/DateHeader.tsx"
      provides: "Date display with navigation arrows and today button"
      exports: ["DateHeader"]
  key_links:
    - from: "native/app/(tabs)/index.tsx"
      to: "native/src/lib/actions/appointments.ts"
      via: "getAppointmentsForRange for data loading"
      pattern: "import.*getAppointmentsForRange.*from.*actions/appointments"
    - from: "native/src/components/diary/DayView.tsx"
      to: "native/src/components/diary/TimeGrid.tsx"
      via: "TimeGrid renders slot rows and appointment blocks"
      pattern: "import.*TimeGrid.*from.*TimeGrid"
    - from: "native/src/components/diary/TimeGrid.tsx"
      to: "native/src/lib/utils/timeGrid.ts"
      via: "generateSlots and appointmentBlockLayout for grid math"
      pattern: "import.*from.*utils/timeGrid"
    - from: "native/src/components/diary/MonthView.tsx"
      to: "react-native-calendars"
      via: "Calendar component with markedDates"
      pattern: "import.*Calendar.*from.*react-native-calendars"
    - from: "native/app/(tabs)/index.tsx"
      to: "native/src/components/diary/SegmentedControl.tsx"
      via: "view state management"
      pattern: "import.*SegmentedControl"
---

<objective>
Build all three calendar views (Month, Week, Day) with the segmented control, date navigation, current time indicator, and auto-scroll.

Purpose: The calendar views are the core of the diary screen — the primary interface the stylist interacts with every day. This plan delivers the visual foundation that booking and appointment management sheets attach to.

Output: A fully navigable diary screen with Month (dot indicators), Week (appointment blocks in 7 columns), and Day (time grid with positioned blocks) views.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-diary-calendar/07-CONTEXT.md
@.planning/phases/07-diary-calendar/07-RESEARCH.md
@.planning/phases/07-diary-calendar/07-01-SUMMARY.md

@native/app/(tabs)/index.tsx
@native/app/(tabs)/_layout.tsx
@native/src/lib/types/database.ts
@native/src/lib/actions/profile.ts
@native/src/theme/colors.ts
@native/src/theme/spacing.ts
@native/src/theme/typography.ts
@native/src/providers/ThemeProvider.tsx

<interfaces>
<!-- From Plan 01 outputs (must exist before this plan runs) -->

From native/src/lib/utils/timeGrid.ts:
```typescript
export const SLOT_HEIGHT_DP = 60
export const TIME_GUTTER_DP = 56
export function parseHHMM(hhmm: string): number
export function generateSlots(startHHMM: string, endHHMM: string, slotMinutes: number): string[]
export function appointmentBlockLayout(appointment: Appointment, gridStartMinutes: number, slotMinutes: number): { top: number; height: number }
export function currentTimeTop(nowMinutes: number, gridStartMinutes: number, slotMinutes: number): number
export function autoScrollTarget(isToday: boolean, nowMinutes: number, workStartMinutes: number): number
```

From native/src/lib/actions/appointments.ts:
```typescript
export function getAppointmentsForRange(startDate: string, endDate: string): Promise<Appointment[]>
export function getAppointmentsForMonth(year: number, month: number): Promise<Appointment[]>
```

From native/src/components/diary/AppointmentBlock.tsx:
```typescript
export function AppointmentBlock(props: {
  appointment: Appointment
  gridStartMinutes: number
  slotMinutes: number
  onPress: () => void
}): JSX.Element
```

From native/src/lib/types/database.ts:
```typescript
export interface Appointment { ... }  // with clients? and appointment_services? joins
export interface Profile { default_slot_minutes: number; working_hours: WorkingHours; ... }
export interface WorkingHours { mon: DaySchedule; ... }
export interface DaySchedule { enabled: boolean; start: string; end: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SegmentedControl, DateHeader, CurrentTimeLine, and TimeGrid shared components</name>
  <files>
    native/src/components/diary/SegmentedControl.tsx,
    native/src/components/diary/DateHeader.tsx,
    native/src/components/diary/CurrentTimeLine.tsx,
    native/src/components/diary/TimeGrid.tsx
  </files>
  <action>
    1. Create `native/src/components/diary/SegmentedControl.tsx`:
       - Pure-JS segmented control (no native module — see RESEARCH.md Pattern 1)
       - Props: `value: 'month' | 'week' | 'day'`, `onChange: (view: CalendarView) => void`
       - Three segments in a row: "Month", "Week", "Day"
       - Active segment: `backgroundColor: theme.card`, `fontWeight: '600'`
       - Container: `backgroundColor: theme.surface`, `borderRadius: 8`, `padding: 2`
       - Each segment: `paddingVertical: 6`, `borderRadius: 6`, `alignItems: 'center'`
       - Export type `CalendarView = 'month' | 'week' | 'day'`

    2. Create `native/src/components/diary/DateHeader.tsx`:
       - Props: `date: Date`, `view: CalendarView`, `onPrev: () => void`, `onNext: () => void`, `onToday: () => void`
       - Display: formatted date string. Day view: "Mon 29 Mar 2026". Week view: "24-30 Mar 2026". Month view: "March 2026".
       - Left arrow (ChevronLeft) and right arrow (ChevronRight) buttons from lucide-react-native
       - Tap on date text returns to today (calls onToday)
       - Minimum 44dp tap targets on arrows
       - If date is today, show subtle "Today" indicator or highlight

    3. Create `native/src/components/diary/CurrentTimeLine.tsx`:
       - Props: `top: number` (pixel position), `gutterWidth: number`
       - Renders absolutely positioned View: red line (`backgroundColor: theme.error`), height 2dp
       - Positioned at `top` pixels, spanning from gutterWidth to right edge
       - Small red circle (6dp) at the left edge (gutter boundary)
       - Use `position: 'absolute'`, `left: gutterWidth - 3`, `right: 0`

    4. Create `native/src/components/diary/TimeGrid.tsx`:
       - Shared component used by both DayView (single column) and WeekView (per-column)
       - Props:
         - `appointments: Appointment[]`
         - `gridStartHHMM: string` (e.g., "06:00")
         - `gridEndHHMM: string` (e.g., "22:00")
         - `slotMinutes: number`
         - `showTimeLabels: boolean` (true for DayView, false for WeekView columns)
         - `onSlotPress?: (slotTime: string) => void` (tap empty slot to start booking)
         - `onAppointmentPress: (appointment: Appointment) => void`
         - `showCurrentTime?: boolean`
         - `isToday?: boolean`
       - Uses `generateSlots()` from timeGrid.ts to compute slot array
       - Renders a fixed-height View (slots.length * SLOT_HEIGHT_DP) with `position: 'relative'`
       - Slot rows: horizontal lines at each slot boundary, time label in gutter (if showTimeLabels)
       - Appointment blocks: map appointments through AppointmentBlock component
       - Current time line: if showCurrentTime && isToday, render CurrentTimeLine at computed position
       - Slot tap handling: TouchableOpacity or Pressable on each slot row, calls onSlotPress with slot time
       - Do NOT use FlatList (appointment blocks need absolute positioning within a fixed container)
       - Memoize slot generation with useMemo
  </action>
  <verify>
    <automated>cd /Users/jamesodwyer/solostylist/native && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - SegmentedControl switches between month/week/day
    - DateHeader shows formatted date with navigation arrows
    - CurrentTimeLine renders red line at correct position
    - TimeGrid renders slot rows and appointment blocks with absolute positioning
  </done>
</task>

<task type="auto">
  <name>Task 2: Build MonthView, WeekView, DayView, and wire DiaryScreen</name>
  <files>
    native/src/components/diary/MonthView.tsx,
    native/src/components/diary/WeekView.tsx,
    native/src/components/diary/DayView.tsx,
    native/app/(tabs)/index.tsx
  </files>
  <action>
    1. Create `native/src/components/diary/MonthView.tsx`:
       - Uses `Calendar` from `react-native-calendars` (see RESEARCH.md Pattern 3)
       - Props: `selectedDate: string` (YYYY-MM-DD), `appointments: Appointment[]`, `onDayPress: (dateString: string) => void`, `onMonthChange: (year: number, month: number) => void`
       - Build `markedDates` from appointments array using `useMemo`:
         - For each appointment, extract date key from `starts_at.split('T')[0]`
         - Mark with `{ marked: true, dotColor: theme.primary }`
         - Selected date additionally gets `{ selected: true, selectedColor: theme.primary }`
       - Theme the Calendar component with theme colours (background, text, arrows, today)
       - On day press: call onDayPress with the date string
       - On month change: call onMonthChange to trigger data reload

    2. Create `native/src/components/diary/WeekView.tsx`:
       - Props: `weekStartDate: Date`, `appointments: Appointment[]`, `slotMinutes: number`, `gridStartHHMM: string`, `gridEndHHMM: string`, `onSlotPress: (date: string, time: string) => void`, `onAppointmentPress: (appointment: Appointment) => void`
       - Layout: 7 columns of equal width filling the screen. No separate horizontal scroll (see RESEARCH.md Pitfall 6).
       - Column width: `(screenWidth - TIME_GUTTER_DP) / 7` (time gutter only on left edge)
       - Header row: day abbreviations + date numbers (Mon 24, Tue 25, etc.)
       - Today's column gets a subtle highlight
       - Each column contains a mini TimeGrid-like layout (but simplified — no time labels per column, just blocks)
       - Use a vertical ScrollView wrapping the entire grid
       - Filter appointments per day column
       - Appointment blocks use AppointmentBlock but with narrower width (column width)
       - Week view shows appointments but text may be very small — show only a coloured bar for very narrow widths
       - Wrap the entire component in GestureDetector with a Pan gesture for swipe navigation (swipe left = next week, swipe right = prev week, threshold 50px). Use `Gesture.Pan()` from react-native-gesture-handler with `runOnJS` for callbacks.

    3. Create `native/src/components/diary/DayView.tsx`:
       - Props: `date: Date`, `appointments: Appointment[]`, `slotMinutes: number`, `gridStartHHMM: string`, `gridEndHHMM: string`, `workStartMinutes: number`, `onSlotPress: (time: string) => void`, `onAppointmentPress: (appointment: Appointment) => void`
       - Renders a ScrollView (ref stored for auto-scroll) containing a single TimeGrid
       - Pass `showTimeLabels: true`, `showCurrentTime: true`, `isToday` computed from date
       - Auto-scroll logic (DIARY-15): in `useEffect` when `date` changes, compute target scroll position:
         - Today: scroll to current time position minus 100dp padding
         - Other days: scroll to working hours start position minus 100dp
         - Use `setTimeout(() => scrollRef.current?.scrollTo(...), 100)` to ensure layout is complete (RESEARCH.md Pitfall 4)
       - Wrap in GestureDetector with Pan gesture for swipe navigation (same pattern as WeekView: swipe left = next day, right = prev day)

    4. Rewrite `native/app/(tabs)/index.tsx` as the main DiaryScreen:
       - State:
         - `view: CalendarView` (default 'day')
         - `selectedDate: Date` (default today)
         - `appointments: Appointment[]` (loaded data)
         - `profile: Profile | null` (for working hours + slot minutes)
       - Data loading: `useFocusEffect` + `useCallback` to load appointments for the current view's date range:
         - Day view: single day (selectedDate to selectedDate+1)
         - Week view: week range (start of week to end of week)
         - Month view: call getAppointmentsForMonth
         - Also reload when selectedDate changes (useEffect)
       - Load profile once for working hours and default_slot_minutes
       - Grid range: "06:00" to "22:00" (fixed, as per CONTEXT.md)
       - Layout:
         - SegmentedControl at top
         - DateHeader below segmented control
         - Conditional view rendering: MonthView, WeekView, or DayView based on `view` state
       - Navigation handlers:
         - onPrev/onNext: advance selectedDate by 1 day (day), 7 days (week), or 1 month (month)
         - onToday: set selectedDate to today
         - Month onDayPress: set selectedDate and switch view to 'day'
       - Hide the default tab header title (set `headerShown: false` in tab screen options or use a custom header)
       - Appointment press handler: store selected appointment in state (used by Plan 05's AppointmentSheet)
       - Slot press handler: store selected slot time (used by Plan 04's BookingSheet)
       - For now, the press handlers can be no-ops or console.log — the sheets are wired in Plans 04 and 05
  </action>
  <verify>
    <automated>cd /Users/jamesodwyer/solostylist/native && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - DiaryScreen shows SegmentedControl with Month/Week/Day
    - Month view shows Calendar with dot indicators for days with appointments
    - Tapping a day in month view switches to day view for that date
    - Week view shows 7 columns with appointment blocks
    - Day view shows time grid with positioned appointment blocks
    - Swipe left/right navigates days (day view) and weeks (week view)
    - Date header shows formatted date with working arrows
    - Tapping date header returns to today
    - Current time red line visible on today's day view
    - Day view auto-scrolls to current time (today) or working hours start
    - Appointments reload when date/view changes
  </done>
</task>

</tasks>

<verification>
- `cd native && npx tsc --noEmit` — no TypeScript errors
- `cd native && npx jest` — existing tests still pass
- Open app > Diary tab shows day view with time grid
- Switch to Month > see dot indicators > tap day > jumps to Day view
- Switch to Week > see 7-column layout
- Swipe left/right in Day view to navigate
- Red current time line visible on today
</verification>

<success_criteria>
- All three calendar views render correctly with real appointment data
- Segmented control switches views without losing selected date
- Navigation (arrows, swipe, tap-to-today) works in all views
- Month view fetches appointments for entire month, not per-day
- Current time indicator on today's day view
- Auto-scroll to correct position
- No nested ScrollView conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/07-diary-calendar/07-03-SUMMARY.md`
</output>
