---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/supabase/server.ts
  - src/lib/supabase/client.ts
  - src/middleware.ts
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/actions.ts
  - src/app/(auth)/check-email/page.tsx
  - src/app/auth/callback/route.ts
  - src/app/onboarding/page.tsx
autonomous: true
requirements: [AUTH-01, DATA-04]

must_haves:
  truths:
    - "User can enter email on login page and submit a magic link request"
    - "User sees a branded 'check your inbox' confirmation after submitting email"
    - "User clicking magic link in email is redirected to /diary (returning) or /onboarding (new)"
    - "Unauthenticated user visiting /diary is redirected to /login"
    - "Authenticated user visiting /login is not blocked (can re-request link)"
  artifacts:
    - path: "src/lib/supabase/server.ts"
      provides: "Server-side Supabase client factory with cookie handling"
      exports: ["createClient"]
    - path: "src/lib/supabase/client.ts"
      provides: "Browser-side Supabase client factory"
      exports: ["createClient"]
    - path: "src/middleware.ts"
      provides: "Auth guard — redirects unauthenticated users to /login"
      contains: "getUser"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Magic link request form with branded UI"
      min_lines: 40
    - path: "src/app/(auth)/login/actions.ts"
      provides: "Server action calling signInWithOtp"
      contains: "signInWithOtp"
    - path: "src/app/(auth)/check-email/page.tsx"
      provides: "Confirmation screen with resend link"
      min_lines: 20
    - path: "src/app/auth/callback/route.ts"
      provides: "Magic link code exchange + new user detection"
      contains: "exchangeCodeForSession"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/app/(auth)/login/actions.ts"
      via: "form action / server action call"
      pattern: "signInWithMagicLink"
    - from: "src/app/(auth)/login/actions.ts"
      to: "src/lib/supabase/server.ts"
      via: "createClient import"
      pattern: "import.*createClient.*from.*supabase/server"
    - from: "src/app/auth/callback/route.ts"
      to: "src/lib/supabase/server.ts"
      via: "createClient import"
      pattern: "import.*createClient.*from.*supabase/server"
    - from: "src/middleware.ts"
      to: "/login"
      via: "redirect on missing user"
      pattern: "NextResponse\\.redirect.*login"
---

<objective>
Implement the complete magic link authentication flow with middleware route protection.

Purpose: Users must be able to sign in securely (AUTH-01) and all business data routes must be inaccessible without authentication (DATA-04). This is the security foundation — every subsequent feature relies on `auth.uid()` being available.

Output: Working auth flow — login page, email submission, "check inbox" screen, magic link callback with new/returning user routing, and middleware that guards all app routes.
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase client factories and auth middleware</name>
  <files>
    src/lib/supabase/server.ts,
    src/lib/supabase/client.ts,
    src/middleware.ts
  </files>
  <action>
    **1. Server-side Supabase client (`src/lib/supabase/server.ts`):**

    Create an async factory function that builds a Supabase server client with cookie access. This is used in Server Components, Server Actions, and Route Handlers.

    ```typescript
    import { createServerClient } from '@supabase/ssr'
    import { cookies } from 'next/headers'

    export async function createClient() {
      const cookieStore = await cookies()  // MUST await — Next.js 15 async API

      return createServerClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
        {
          cookies: {
            getAll() {
              return cookieStore.getAll()
            },
            setAll(cookiesToSet) {
              try {
                cookiesToSet.forEach(({ name, value, options }) =>
                  cookieStore.set(name, value, options)
                )
              } catch {
                // Server Component context — cookie writes ignored; middleware handles token refresh
              }
            },
          },
        }
      )
    }
    ```

    **2. Browser-side Supabase client (`src/lib/supabase/client.ts`):**

    ```typescript
    import { createBrowserClient } from '@supabase/ssr'

    export function createClient() {
      return createBrowserClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
      )
    }
    ```

    **3. Auth middleware (`src/middleware.ts`):**

    Place at `src/middleware.ts` (inside src/ directory). This runs on every request except static assets.

    - Create Supabase server client with request/response cookie bridge
    - Call `supabase.auth.getUser()` — NEVER `getSession()` (security risk in server context, see RESEARCH.md Pitfall 1)
    - Define auth routes that don't require authentication: `/login`, `/check-email`, `/auth/callback`
    - If no user AND not on an auth route → redirect to `/login`
    - If user exists AND on `/login` → do NOT redirect (user can re-request link)
    - Return the supabaseResponse (carries refreshed cookies)

    Matcher config — exclude static assets:
    ```typescript
    export const config = {
      matcher: [
        '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
      ],
    }
    ```

    Follow the exact middleware pattern from RESEARCH.md Pattern 1, using `request.cookies.getAll()` / `request.cookies.set()` / `supabaseResponse.cookies.set()` for the cookie bridge.
  </action>
  <verify>
    <automated>cd /Users/jamesodwyer/solostylist && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `src/lib/supabase/server.ts` exports async `createClient()` using `createServerClient` with `await cookies()`
    - `src/lib/supabase/client.ts` exports `createClient()` using `createBrowserClient`
    - `src/middleware.ts` calls `getUser()` (not `getSession()`), redirects unauthenticated users to `/login`, excludes auth routes from protection
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build magic link login flow with branded UI</name>
  <files>
    src/app/(auth)/login/page.tsx,
    src/app/(auth)/login/actions.ts,
    src/app/(auth)/check-email/page.tsx,
    src/app/auth/callback/route.ts,
    src/app/onboarding/page.tsx
  </files>
  <action>
    **1. Login server action (`src/app/(auth)/login/actions.ts`):**

    Create a `'use server'` action `signInWithMagicLink(email: string)`:
    - Import `createClient` from `@/lib/supabase/server`
    - Call `supabase.auth.signInWithOtp({ email, options: { emailRedirectTo: \`\${process.env.NEXT_PUBLIC_SITE_URL}/auth/callback\` } })`
    - If error, throw it (or return an error object — prefer returning `{ error: string }` for form handling)
    - On success, redirect to `/check-email`

    **2. Login page (`src/app/(auth)/login/page.tsx`):**

    Branded, warm login screen per user decisions:
    - Full-bleed black background (monochrome brand — black/white theme, so "brand color" = black)
    - Large "SoloStylist" text/logo at top, prominent and centred
    - Friendly copy below: "Welcome back, gorgeous" (per CONTEXT.md)
    - Email input field with white/light styling against black background
    - Submit button: "Send magic link" — white button on black background
    - Minimal, clean layout. No clutter.
    - Use a client component (`'use client'`) for the form, or a Server Component with a form that calls the server action. Prefer: Server Component page that renders a client component `LoginForm` for interactive state (loading, error display).
    - Show loading state on button during submission
    - Show error message if signInWithOtp fails
    - Mobile-first: full width inputs, 44px+ tap targets, appropriate spacing

    Layout note: The `(auth)` route group does NOT use the app shell layout (no bottom nav). It gets its own minimal layout or just uses the root layout.

    **3. Check-email page (`src/app/(auth)/check-email/page.tsx`):**

    Dedicated confirmation screen per user decisions:
    - New page (not a modal or toast)
    - Centred layout with icon or illustration (use a simple mail icon from lucide-react)
    - "Check your inbox" heading, large and friendly
    - Subtext: "We've sent a magic link to your email. Tap it to sign in."
    - "Resend" link/button that navigates back to `/login` (simple approach) or calls the server action again
    - "Back to login" secondary link
    - Should feel like a deliberate step, not a dead end (per CONTEXT.md specifics)
    - Match the visual style of the login page (black/white theme)

    **4. Auth callback route (`src/app/auth/callback/route.ts`):**

    Route Handler (not a page) that exchanges the magic link code for a session:
    - Extract `code` from searchParams
    - Call `supabase.auth.exchangeCodeForSession(code)` — this validates the PKCE code and sets session cookies
    - After successful exchange, detect new vs returning user:
      - Call `supabase.auth.getUser()` to get user
      - Query `profiles` table: `supabase.from('profiles').select('onboarding_completed').eq('id', user.id).single()`
      - If `onboarding_completed` is false → redirect to `/onboarding`
      - If `onboarding_completed` is true → redirect to `/diary`
    - If code is missing or exchange fails → redirect to `/login?error=auth_callback_failed`

    **5. Onboarding placeholder (`src/app/onboarding/page.tsx`):**

    Lightweight placeholder page (Phase 2 builds the full business profile setup):
    - Simple centred layout
    - "Welcome to SoloStylist!" heading
    - Brief text: "Let's get your business set up."
    - For now, just a "Continue" button/link that navigates to `/diary` (Phase 2 will replace this with the real onboarding flow)
    - This page is NOT behind the (app) route group — it has its own route so it's accessible right after auth callback

    **Route group note:** The `(auth)` route group wraps login and check-email. These pages should NOT have the bottom navigation bar. They use the root layout only. The `auth/callback` route is outside the group (it's a Route Handler, not a page). The `onboarding` page is also outside the (app) group.
  </action>
  <verify>
    <automated>cd /Users/jamesodwyer/solostylist && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `/login` renders branded login page with email input and "Send magic link" button on black background with "Welcome back, gorgeous" copy
    - `/check-email` renders confirmation page with mail icon, "Check your inbox" heading, and resend link
    - `/auth/callback` exchanges code for session and redirects to `/onboarding` (new user) or `/diary` (returning user)
    - `/onboarding` renders placeholder welcome page
    - `npm run build` succeeds with all routes compiling
    - Unauthenticated visit to `/diary` redirects to `/login` (middleware working)
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds — all routes compile
2. `npx tsc --noEmit` — no TypeScript errors
3. Middleware correctly identifies auth routes vs protected routes
4. Login page uses branded monochrome styling with friendly copy
5. Check-email page is a distinct page (not inline on login)
6. Auth callback handles both new and returning user routing
</verification>

<success_criteria>
- Supabase client factories exist for both server and browser contexts
- Middleware uses `getUser()` (never `getSession()`) and redirects unauthenticated users
- Login page has branded UI: black background, logo, "Welcome back, gorgeous" copy, email input
- Check-email page is a deliberate confirmation step with resend option
- Auth callback exchanges magic link code and routes new users to onboarding, returning users to diary
- All code compiles without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
